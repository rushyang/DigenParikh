cp ????? "$HOME"

#$HOME is the environment variable. Already exist in 'env'. Which will hold each user's HOME directory by default.
# single ? indicates the name of a file can be any single character.
